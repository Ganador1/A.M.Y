# Preregistered hint H1 — growing degree

Generalize the Gaussian-lattice construction from `Q(i)` to number fields whose
degrees grow. Preserve the exact asymptotic quantifiers, distinctness, and pair
multiplicities. This is only a direction: do not assume it succeeds, and reject it
if its constants or embeddings cannot be controlled uniformly.
